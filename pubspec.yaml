name: image_editor
description: "Image_editor is an embeddable Flutter image editing component that offers common editing tools such as cropping, rotating, and text overlay."
version: 0.0.1
homepage: https://github.com/xinqingaa/image_editor
repository: https://github.com/xinqingaa/image_editor


environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

